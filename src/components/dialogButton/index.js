import React from 'react';


const DialogButton = ({onClick}) => {

    return (
    <button onClick={onClick}>Open File Dialog</button>
    // how to open file in server with electron


    );  
}



export default DialogButton;